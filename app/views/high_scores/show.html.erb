<p id="notice"><%= notice %></p>

<div id=main>
  <form method="put" action="/high_scores">

    <input type='button' id="new" value="Click for new plot">
  <p>
  </p>

  <input type="hidden" id="this_id" value=<%= "#{@high_score.id}" %>>


<!--    <a href="/high_scores/<%#= 2 %>">Update Plot</a> -->
  <!--
       can uncomment below, and we do get an alert success, but $( "#main").html( data) doesn't do what we want,
       i.e. the whole page still redraws.
    -->
  <br/>
  <br/>
  <br/>
 <!-- <%#= link_to 'home',  '/' %>   -->
  </form>
</div>

<script type="text/javascript">
    $(function(){

        var data_0 = <%= @data_cont%>
        var data_1 = <%= @data_cont_1%>
       init(data_0);
       //redraw(data_1)
          /*
        $( "#update_data" ).click(function(){
            //alert ("clicked " + this.name)
            $.ajax({
                type: 'GET',
                url: '/high_scores/' + this.name ,
                data: { postVar1: 'theValue1', postVar2: 'theValue2' },
                success:function(data){
                    // alert("success")
                    // successful request; do something with the data
                    $('#ajax-panel').empty();
                    $( "#main").html( data)
                },
                error:function(){
                    // failed request; give feedback to user
                    $('#ajax-panel').html('<p class="error"><strong>Error!</strong> Try again in a few moments.</p>');
                }
            });
        });
        */

    });

</script>